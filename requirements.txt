# Core dependencies
certifi>=2024.0.0
truststore>=0.9.0
fastapi>=0.115.0
uvicorn>=0.25.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.28.0

# LangChain ecosystem
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-classic>=1.0.0  # langchain_classic.chains.base.Chain — used by playwright_navigator
langchain-cohere>=0.3.0
langchain-anthropic>=0.3.0
langchain-aws>=0.2.0
langchain-openai>=0.3.0  # exo provider — generic ChatOpenAI client against /v1
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
deepagents

# MLX local inference (Apple Silicon)
mlx>=0.22.0
mlx-lm>=0.31.0
mlx-vlm>=0.3.12
transformers>=5.0.0,<5.13.0  # 5.13.0 broke mlx-lm's AutoTokenizer.register("NewlineTokenizer", ...) string-key call
huggingface-hub>=1.3.0

# Wikipedia
wikipedia>=1.4.0

# YouTube transcripts (langchain_community.YoutubeLoader backend)
youtube-transcript-api>=1.0.0

# macOS desktop automation (Apple Silicon / macOS only)
pyautogui>=0.9.54
pyobjc-core>=10.0
pyobjc-framework-Cocoa>=10.0        # AppKit, Foundation
pyobjc-framework-ApplicationServices>=10.0  # Accessibility API (AXUIElement*)
pyobjc-framework-Vision>=10.0       # on-device OCR (VNRecognizeTextRequest) for AX-disabled apps
pyobjc-framework-Quartz>=10.0       # CGWindowList — window bounds without Accessibility (AX-off apps)

# Browser automation
playwright>=1.40.0
aiohttp>=3.9.0

# Image processing
Pillow>=10.0.0

# MCP server for Claude Desktop
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0

# Token splitters
tiktoken>=0.12.0

# Observability / tracing
traceloop-sdk>=0.33.0

# sqlite
aiosqlite

# Nested event loops (Jupyter, web loader when loop already running)
nest-asyncio>=1.6.0

# strenum
strenum

# draw langgraphs (draw_ascii works without these)
grandalf

# Anthropic SDK
anthropic>=0.40.0

# Research tools (web/doc researcher)
ddgs>=9.0.0
markdownify>=0.13.0
rank-bm25>=0.2.0
langchain-text-splitters>=0.3.0
pypdf>=3.0.0
python-docx>=1.0.0
python-pptx>=1.0.0
openpyxl>=3.1.0

# Evaluation
deepeval>=2.0.0

# Remote transcript access (OpenClaw SSH)
asyncssh>=2.17.0

# JWT decoding
PyJWT>=2.0.0

# WebSocket (backend streaming)
websockets>=14.0

# Scheduler (backend cron jobs)
APScheduler>=3.10.0,<4

# Credential vault — wraps macOS Keychain / Linux Secret Service / Windows Credential Manager
keyring>=25.0.0
